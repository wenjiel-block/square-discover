import SwiftUI

extension Color {
    /// Warm orange - primary brand color.
    static let brandPrimary = Color(red: 0.95, green: 0.55, blue: 0.15)

    /// Deep brown - secondary brand color.
    static let brandSecondary = Color(red: 0.30, green: 0.18, blue: 0.10)

    /// Coral/red - accent color for CTAs and highlights.
    static let brandAccent = Color(red: 0.93, green: 0.30, blue: 0.25)

    /// Primary surface color for cards and backgrounds.
    static let surfacePrimary = Color(.systemBackground)

    /// Secondary surface color for grouped backgrounds.
    static let surfaceSecondary = Color(.secondarySystemBackground)
}
